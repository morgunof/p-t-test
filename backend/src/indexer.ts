// Minimal indexer stub — расширим при интеграции с TON
console.log('Indexer started (stub). Configure TON RPC + listeners later.');
setInterval(() => {
  console.log('[indexer] heartbeat');
}, 15000);
